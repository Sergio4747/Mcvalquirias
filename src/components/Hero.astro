<section class="hero">
  <video autoplay muted loop playsinline class="background-video">
    <source src="/video/herovid.mp4" type="video/mp4" />
    Tu navegador no soporta video HTML5.
  </video>

  <div class="hero-overlay">
    <div class="hero-content">
      <h1>Valquirias Estética</h1>
      <p>Realzá tu belleza con manos profesionales.</p>
      <a href="/servicios" class="boton-cta">Ver servicios</a>
    </div>
  </div>
</section>

<style>
  :global(*) {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  :global(html),
  :global(body) {
    width: 100%;
    height: 100%;
    overflow-x: hidden;
    font-family: "Poppins", sans-serif;
    background-color: #000;
  }

  .hero {
    position: relative;
    width: 100vw;
    height: 100dvh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .background-video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 1;
    filter: brightness(0.75);
    pointer-events: none;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    z-index: 2;
    background: linear-gradient(
      to bottom,
      rgba(162, 92, 207, 0.4),
      rgba(0, 0, 0, 0.6)
    );
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem;
    text-align: center;
  }

  .hero-content {
    z-index: 3;
    max-width: 720px;
    padding: 1rem;
    color: #ffffff;
    animation: fadeInUp 1.2s ease both;
  }

  .hero h1 {
    font-size: clamp(2.5rem, 6vw, 3.5rem);
    color: #e96ee3;
    font-weight: 700;
    margin-bottom: 1rem;
    text-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
  }

  .hero p {
    font-size: clamp(1.1rem, 2.5vw, 1.4rem);
    color: #f0f0f0;
    margin-bottom: 2rem;
    line-height: 1.6;
    text-shadow: 0 1px 4px rgba(0, 0, 0, 0.25);
  }

  .boton-cta {
    background-color: #d4af37;
    color: white;
    padding: 14px 32px;
    font-size: 1rem;
    font-weight: 600;
    border-radius: 50px;
    text-decoration: none;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
    display: inline-block;
  }

  .boton-cta:hover {
    background-color: #b8922c;
    transform: scale(1.05);
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Responsive – celulares */
  @media (max-width: 480px) {
    .hero h1 {
      font-size: 2.4rem; /* antes 1.6rem */
    }

    .hero p {
      font-size: 1.15rem; /* antes 0.95rem */
    }

    .boton-cta {
      padding: 14px 28px; /* más grande y cómodo */
      font-size: 1rem; /* más legible */
    }
  }

  @media (max-width: 480px) {
    .hero h1 {
      font-size: 2.8rem !important;
    }

    .hero p {
      font-size: 1.5rem !important;
    }

    .boton-cta {
      font-size: 1.2rem !important;
      padding: 16px 30px !important;
    }
  }
  @media (max-width: 768px) {
    .hero {
      max-height: 100dvh;
      overflow: hidden;
      overscroll-behavior: contain;
    }
  }
</style>
